DirectoryIndex index.php
Options -Indexes

<FilesMatch "^(config\.php|config\.example\.php|app\.php)$">
    Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^storage(?:/|$) - [F,L]
</IfModule>
